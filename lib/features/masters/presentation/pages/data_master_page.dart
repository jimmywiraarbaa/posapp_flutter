import 'package:flutter/material.dart';

class DataMasterPage extends StatelessWidget {
  const DataMasterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Data Master')),
    );
  }
}
